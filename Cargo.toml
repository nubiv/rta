[package]
name = "rta"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = "4.5.41"
plotly = { version = "0.13.4", features = ["kaleido"] }
polars = { version = "0.49.1", features = ["lazy", "parquet", "csv"] }
